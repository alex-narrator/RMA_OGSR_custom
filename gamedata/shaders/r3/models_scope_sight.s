function normal(shader, t_base, t_second, t_detail)
	shader:begin("model_def_lplanes", "models_scope_sight")
	: fog(true)
	: zb(true, false)
	: blend(true, blend.srcalpha, blend.invsrcalpha)
	: aref(true, 0)
	: sorting(2, true)
	: distort(true)
	shader:dx10texture("s_base", t_base)
	shader:dx10texture("s_vp2",	"$user$generic0_temp")
	shader:dx10texture("s_inside", "wpn\\scope_utility\\inside")
	shader:dx10texture("s_dirt", "wpn\\scope_utility\\dirt")
	shader:dx10texture("s_lens", "wpn\\wpn_addons\\wpn_addon_scope_wa2000\\wpn_addon_scope_wa2000_lens")
	shader:dx10sampler("smp_base")	
    //shader:dx10stencil(true, cmp_func.always, 2, 2, stencil_op.replace, stencil_op.replace, stencil_op.replace)
    //shader:dx10stencil_ref(2)
end
